[< Back to MS-FSCC Index](../INDEX.md)

---

### 2.3.5 FSCTL_DELETE_REPARSE_POINT Request

This message requests that the server delete the [**reparse
point**](#gt_4fed0b53-5fc8-4818-886f-93d87f3035e1) from the file or
directory associated with the handle on which this
[**FSCTL**](#gt_4ffb96a7-5fad-488e-9438-b7707d2e4226) was invoked. The
underlying file or directory MUST NOT be deleted.

The message MUST contain a
[REPARSE_GUID_DATA_BUFFER](#Section_a4d083740e9243e28f8888b94112f070) or
a [REPARSE_DATA_BUFFER](#Section_c3a420cb8a724adf87e8eee95379d78f) data
element (including subtypes). Both the REPARSE_GUID_DATA_BUFFER and the
REPARSE_DATA_BUFFER structures begin with a **ReparseTag** field. The
**ReparseTag** value uniquely identifies the
[**filter**](#gt_ffbe7b55-8e84-4f41-a18d-fc29191a4cda) driver that
creates/uses the reparse point, and the application's filter driver
processes the reparse point data as either a REPARSE_GUID_DATA_BUFFER or
a REPARSE_DATA_BUFFER, depending on the structure implemented by the
filter driver for that type of reparse point.

This message MUST only be sent for a file or directory handle.

